# Project metadata, dependencies, entry points, and pytest configuration have
# been moved to pyproject.toml for uv compatibility.
# This file is kept for any setuptools-specific options not expressible in
# pyproject.toml (none currently).
